import Controller from './cart.controller';

const COMPONENT: ng.IComponentOptions = {
    templateUrl: '/public/app/cart/cart.template.html',
    controller: Controller,
}

export default COMPONENT;
export const NAME: string = 'vcShoppingCart';